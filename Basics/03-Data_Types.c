#include<stdio.h>
int main ()
{
    int a = 2;

    float b = 1.4;

    char c =  '#'; // Charecter must be in single inverted comma

    printf("the value of a is %d\n",a);
    printf("the value of b is %f\n",b);// if we want 2 digit after the point then we'll add (%.2f)
    printf("the value of c is %c\n",c);
    return 0;

}