#include<stdio.h>
// type casting means changing the variable type as you want

int main(){

    // if we want to change the 31.03 float to 31 in int then
    int n ;
    float m = 31.03;
    n = (int)m ;// in the perentesis the type we want to get willl sit
    printf("%d",n);


return 0;
}