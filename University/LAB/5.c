#include<stdio.h>
void prime(int x){
    if (x%1==0 && x%x==0){
        printf("%d is prime.",x);
    }
}
void arm(int x){
    if(x );
}

int main(){

    int x;
    printf("Input any number1:");
    scanf("%d",&x);
    prime(x);

return 0;
}